import React from 'react';
import styles from './join.module.scss';

const Join = () => {
    return (
        <div className={styles.Join}>
            <p>JOIN</p>
        </div>
    );
};

export default Join;
